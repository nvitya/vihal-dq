
// the user project must provide the "board" module, which should define the MCU registers and MCU defines !

use ^/core/mcu_defs   reexport  // board module used and reexported inside

#ifdef CPU_ARMM
    use ^/armm/core/armm_utils   reexport
#endif

// Common variables

#ifndef MCU_INTERNAL_RC_SPEED
    const MCU_INTERNAL_RC_SPEED : uint = 4000000
#endif

var SystemCoreClock : uint  [[section('.noinit')]]

use ^/core/c_system

//-----------------------------------------------------------------------------

implementation